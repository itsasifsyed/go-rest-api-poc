package router

// v1 routes
// mux.HandleFunc("/v1/users", userHandler.RegisterUser)
// mux.HandleFunc("/v1/users/get", userHandler.GetUser)
// mux.HandleFunc("/v1/products", productHandler.CreateProduct)
// mux.HandleFunc("/v1/products/get", productHandler.GetProduct)

// v2 routes (example: new version of user endpoint)
// mux.HandleFunc("/v2/users", userHandler.RegisterUserV2)
// mux.HandleFunc("/v2/products", productHandler.CreateProductV2)
